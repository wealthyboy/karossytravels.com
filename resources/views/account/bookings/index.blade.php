@extends('layouts.public')
@section('title', 'My bookings')
@section('content')
<main class="public-account-page"><div class="container py-5"><div class="d-flex justify-content-between align-items-end mb-4"><div><p class="text-danger fw-semibold mb-1">MY TRIPS</p><h1 class="h3 mb-1">My bookings</h1><p class="text-secondary mb-0">Flights and hotel stays booked through your account.</p></div><a href="{{ route('home') }}" class="btn btn-karossy">Book another trip</a></div>
<div class="row g-3">@forelse($bookings as $booking)<div class="col-12"><a class="card text-decoration-none text-dark border-0 shadow-sm" href="{{ route('account.bookings.show', $booking) }}"><div class="card-body d-flex flex-wrap justify-content-between align-items-center gap-3"><div><small class="text-danger fw-semibold">{{ strtoupper($booking->product_type) }}</small><h2 class="h5 mb-1">{{ $booking->order?->reference }}</h2><span class="text-secondary">{{ $booking->provider_locator ? 'Booking locator: '.$booking->provider_locator : 'Awaiting confirmation' }}</span></div><div class="text-end"><strong class="d-block">{{ $booking->order?->currency }} {{ number_format(($booking->order?->total_minor ?? 0)/100, 2) }}</strong><span class="badge {{ $booking->status === 'confirmed' ? 'text-bg-success' : 'text-bg-warning' }}">{{ ucfirst($booking->status) }}</span></div></div></a></div>@empty<div class="col-12"><div class="alert alert-light border">You have no bookings yet.</div></div>@endforelse</div><div class="mt-4">{{ $bookings->links() }}</div></div></main>
@endsection
