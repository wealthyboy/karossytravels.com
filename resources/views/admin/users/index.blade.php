@extends('layouts.admin')

@section('title', 'Accounts')

@section('content')
<header class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4"><div><p class="text-danger fw-semibold mb-1">ACCOUNT MANAGEMENT</p><h1 class="h3 fw-bold mb-1">Accounts</h1><p class="text-secondary mb-0">Manage B2C, B2B and administrative user accounts.</p></div><a href="{{ route('admin.users.create') }}" class="btn btn-karossy"><i class="bi bi-plus-lg me-2"></i>New account</a></header>
<div class="card content-card admin-table-card"><div class="card-header bg-white border-0 p-3 p-md-4"><form method="GET" class="d-flex gap-2" autocomplete="off"><div class="input-group admin-table-search"><span class="input-group-text"><i class="bi bi-search"></i></span><input name="q" value="{{ request('q') }}" class="form-control" placeholder="Search name, email or company" autocomplete="off"></div><button class="btn admin-search-button">Search</button></form></div><div class="table-responsive"><table class="table admin-data-table align-middle mb-0"><thead><tr><th class="ps-4">User</th><th>Account type</th><th>Company</th><th>Currency</th><th>Status</th><th>Roles</th></tr></thead><tbody>@forelse($users as $user)<tr><td class="ps-4"><strong class="d-block">{{ $user->name }}</strong><small class="text-secondary">{{ $user->email }}</small></td><td><span class="badge rounded-pill {{ $user->account_type === 'b2b' ? 'badge-system' : 'badge-custom' }}">{{ strtoupper($user->account_type) }}</span></td><td>{{ $user->company_name ?: '—' }}</td><td><code>{{ $user->currency_code }}</code></td><td><span class="status-dot {{ $user->status === 'active' ? 'ready' : 'pending' }}"></span>{{ ucfirst($user->status) }}</td><td><span class="count-pill">{{ $user->roles_count }}</span></td></tr>@empty<tr><td colspan="6" class="text-center text-secondary py-5">No users found.</td></tr>@endforelse</tbody></table></div><div class="card-footer bg-white border-0 p-3 p-md-4">{{ $users->links() }}</div></div>
@endsection
