@extends('layouts.admin')
@section('title', 'Visa applications')
@section('content')
<header class="admin-page-heading d-flex flex-column flex-md-row justify-content-between align-items-md-end gap-3"><div><span class="admin-eyebrow">VISA OPERATIONS</span><h1>Visa applications</h1><p>Track paid applications, document collection and processing outcomes.</p></div><form method="GET" class="d-flex gap-2"><select class="form-select" name="status"><option value="">All statuses</option>@foreach(['awaiting_payment','submitted','documents_requested','processing','approved','rejected','cancelled'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ str($status)->headline() }}</option>@endforeach</select><button class="btn admin-search-button">Filter</button></form></header>
<div class="card content-card admin-table-card"><div class="table-responsive"><table class="table admin-data-table align-middle mb-0"><thead><tr><th class="ps-4">Reference</th><th>Visa route</th><th>Customer</th><th>Travellers</th><th>Total</th><th>Status</th><th class="text-end pe-4">Action</th></tr></thead><tbody>@forelse($applications as $application)<tr><td class="ps-4"><strong>{{ $application->reference }}</strong><small class="d-block text-secondary">{{ $application->created_at->format('d M Y, H:i') }}</small></td><td>{{ $application->visa->passport_country }} → {{ $application->visa->country }}</td><td><strong>{{ data_get($application->contact,'name') }}</strong><small class="d-block text-secondary">{{ data_get($application->contact,'email') }}</small></td><td>{{ $application->travellers }}</td><td>{{ $application->currency }} {{ number_format($application->total_minor/100,2) }}</td><td><span class="status-pill status-active">{{ str($application->status)->headline() }}</span></td><td class="text-end pe-4"><a class="table-action-btn" href="{{ route('admin.visa-applications.show',$application) }}"><i class="bi bi-eye"></i><span>Review</span></a></td></tr>@empty<tr><td colspan="7" class="text-center text-secondary py-5">No visa applications found.</td></tr>@endforelse</tbody></table></div>@if($applications->hasPages())<div class="card-footer bg-white border-0 p-4">{{ $applications->links() }}</div>@endif</div>
@endsection
