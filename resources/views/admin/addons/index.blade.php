@extends('layouts.admin')
@section('title', ucfirst($type ?? 'All').' Add-ons')
@section('content')
@php $query=['type'=>$type];$sortUrl=fn(string $column)=>route('admin.addons.index',array_merge(request()->query(),['sort'=>$column,'direction'=>request('sort')===$column&&request('direction')!=='desc'?'desc':'asc'])); @endphp
<header class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4"><div><p class="text-danger fw-semibold mb-1">{{ strtoupper($type ?? 'CHECKOUT') }}</p><h1 class="h3 fw-bold mb-1">{{ $type ? ucfirst($type) : 'Travel' }} add-ons</h1><p class="text-secondary mb-0">Optional services customers can select during checkout.</p></div><a class="btn btn-karossy" href="{{ route('admin.addons.create',$query) }}"><i class="bi bi-plus-lg"></i> New add-on</a></header>
<div class="card content-card admin-table-card"><div class="card-header bg-white border-0 p-3 p-md-4"><form method="GET" class="d-flex gap-2">@if($type)<input type="hidden" name="type" value="{{ $type }}">@endif<div class="input-group admin-table-search"><span class="input-group-text"><i class="bi bi-search"></i></span><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Search add-ons"></div><button class="btn admin-search-button">Search</button></form></div><div class="table-responsive"><table class="table admin-data-table align-middle mb-0"><thead><tr><th class="ps-4">Image</th><th><a href="{{ $sortUrl('title') }}">Add-on <i class="bi bi-arrow-down-up"></i></a></th><th>Product</th><th><a href="{{ $sortUrl('price_cents') }}">Price <i class="bi bi-arrow-down-up"></i></a></th><th>Status</th><th class="text-end pe-4">Actions</th></tr></thead><tbody>@forelse($addons as $addon)<tr><td class="ps-4">@if($addon->image_path)<img src="{{ Storage::url($addon->image_path) }}" class="rounded-3 object-fit-cover" width="48" height="48" alt="">@else<span class="brand-mark"><i class="bi bi-bag-plus"></i></span>@endif</td><td><strong>{{ $addon->title }}</strong><small class="d-block text-secondary text-truncate" style="max-width:30rem">{{ $addon->description }}</small></td><td><span class="badge rounded-pill badge-custom">{{ ucfirst($addon->type) }}</span></td><td>{{ $addon->currency }} {{ number_format($addon->price_cents/100,2) }}</td><td><span class="status-dot {{ $addon->active?'ready':'pending' }}"></span>{{ $addon->active?'Active':'Inactive' }}</td><td class="text-end pe-4"><div class="table-actions"><a class="btn table-action-btn" href="{{ route('admin.addons.edit',$addon) }}"><i class="bi bi-pencil"></i><span>Edit</span></a><form method="POST" action="{{ route('admin.addons.destroy',$addon) }}" data-confirm="Delete this add-on?">@csrf @method('DELETE')<button class="btn table-action-btn table-action-danger"><i class="bi bi-trash3"></i><span>Delete</span></button></form></div></td></tr>@empty<tr><td colspan="6" class="text-center text-secondary py-5">No add-ons found.</td></tr>@endforelse</tbody></table></div><div class="card-footer bg-white border-0 p-3 p-md-4 d-flex justify-content-between"><small class="text-secondary">{{ $addons->total() }} add-ons</small>{{ $addons->links() }}</div></div>
@endsection
