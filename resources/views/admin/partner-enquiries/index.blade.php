@extends('layouts.admin')
@section('title', 'Car partner enquiries')
@section('content')
<header class="admin-page-heading d-flex flex-column flex-md-row justify-content-between align-items-md-end gap-3"><div><span class="admin-eyebrow">CAR PARTNER NETWORK</span><h1>Driver applications</h1><p>Review people and vehicle operators who want to join the Karossy transport network.</p></div><form method="GET" class="d-flex gap-2"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Name, city or email"><button class="btn admin-search-button">Search</button></form></header>
<div class="card content-card admin-table-card"><div class="table-responsive"><table class="table admin-data-table align-middle mb-0"><thead><tr><th class="ps-4">Applicant</th><th>Location</th><th>Vehicle</th><th>Submitted</th><th>Status</th><th class="text-end pe-4">Action</th></tr></thead><tbody>@forelse($enquiries as $enquiry)<tr><td class="ps-4"><strong>{{ $enquiry->name }}</strong><small class="d-block text-secondary">{{ $enquiry->email }} · {{ $enquiry->phone }}</small></td><td>{{ $enquiry->city }}</td><td>{{ $enquiry->vehicle_type }} @if($enquiry->vehicle_year)<small class="text-secondary">({{ $enquiry->vehicle_year }})</small>@endif</td><td>{{ $enquiry->created_at->format('d M Y') }}</td><td><span class="status-pill status-active">{{ str($enquiry->status)->headline() }}</span></td><td class="text-end pe-4"><a class="table-action-btn" href="{{ route('admin.partner-enquiries.show',$enquiry) }}"><i class="bi bi-eye"></i><span>Review</span></a></td></tr>@empty<tr><td colspan="6" class="text-center text-secondary py-5">No driver applications found.</td></tr>@endforelse</tbody></table></div>@if($enquiries->hasPages())<div class="card-footer bg-white border-0 p-4">{{ $enquiries->links() }}</div>@endif</div>
@endsection
